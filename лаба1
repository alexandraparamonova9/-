nums = [2, 7, 11, 15] #список чисел
target = 9 #число суммы двух чисел списка
def twosum(nums, target):
    for i in range(len(nums)):
        for j in range(i + 1, len(nums)):
            if nums[i] + nums[j] == target:
                return [i, j]
output = twosum(nums, target)
print(output)
